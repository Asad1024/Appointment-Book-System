{
  "name": "appointment-booking-system",
  "private": true,
  "version": "1.0.0",
  "description": "Production appointment booking platform",
  "scripts": {
    "dev": "concurrently -n API,WEB -c cyan,magenta \"npm run dev --prefix apps/api\" \"npm run dev --prefix apps/web\"",
    "setup:local": "npm install && npm run db:generate && npm run db:push && npm run db:seed",
    "build": "npm run build --prefix apps/api && npm run build --prefix apps/web",
    "test": "npm test --prefix apps/api",
    "lint": "npm run lint --prefix apps/api && npm run lint --prefix apps/web",
    "db:generate": "npm run prisma:generate --prefix apps/api",
    "db:migrate": "npm run prisma:migrate --prefix apps/api",
    "db:seed": "npm run prisma:seed --prefix apps/api",
    "db:push": "npm run prisma:push --prefix apps/api",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.0.0"
}
